{
  "name": "wattanar.github.io",
  "private": true,
  "scripts": {
    "build": "cross-env JEKYLL_ENV=production bundle exec jekyll build",
    "serve": "bundle exec jekyll serve -H 0.0.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.0"
  }
}
